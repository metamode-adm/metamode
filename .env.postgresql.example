# ========================================
# CONFIGURAÇÃO PARA PostgreSQL
# ========================================
# Copie este arquivo para .env e ajuste os valores conforme sua instalação PostgreSQL

# ========================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ========================================
# Tipo de banco de dados (OBRIGATÓRIO para PostgreSQL)
DB_TYPE=postgresql

# Credenciais do banco de dados PostgreSQL
# IMPORTANTE: Crie um usuário específico para o Metamode em produção
# Exemplo: CREATE USER metamode_user WITH PASSWORD 'senha_forte';
#          CREATE DATABASE metamode OWNER metamode_user;
#          GRANT ALL PRIVILEGES ON DATABASE metamode TO metamode_user;
DB_USER="postgres"
DB_PASSWORD="password"
DB_HOST="127.0.0.1"
DB_PORT=5432
DB_NAME=metamode

# ========================================
# CONFIGURAÇÕES DE SEGURANÇA
# ========================================
# Chave secreta para criptografia de sessões (OBRIGATÓRIO alterar em produção)
# Gere uma chave forte com: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_postgresql_secret_key_here

# ========================================
# CONFIGURAÇÕES DO SISTEMA
# ========================================
# Tamanho máximo de upload em MB (padrão: 500MB)
MAX_UPLOAD_SIZE_MB=500

# Fuso horário padrão do sistema
DEFAULT_TIMEZONE=America/Sao_Paulo

# Tempo de expiração da sessão de admin em segundos (padrão: 1 hora)
ADMIN_SESSION_EXPIRATION=3600

# Duração de cada mídia no carrossel em segundos (padrão: 5 segundos)
CAROUSEL_MEDIA_DURATION=5

# ========================================
# CONFIGURAÇÕES DE LOGS E DIRETÓRIOS
# ========================================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Diretório para armazenar uploads (será criado automaticamente)
UPLOAD_DIR=uploads

# Diretório para armazenar logs (será criado automaticamente)
LOG_DIR=logs

# ========================================
# AMBIENTE DE EXECUÇÃO
# ========================================
# 'production' para produção ou 'development' para desenvolvimento
# Em desenvolvimento, mostra mais detalhes de erro e recarrega automaticamente
ENVIRONMENT=production